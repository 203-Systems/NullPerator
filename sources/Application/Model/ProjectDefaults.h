/*
 * SPDX-License-Identifier: BSD-3-Clause
 */

#pragma once

// Project defaults are part of the project/model contract and must not change
// with the UI or platform adapter selected at build time.
inline constexpr int DEFAULT_MASTER_VOLUME = 60;
inline constexpr unsigned short MIN_TEMPO = 60;
inline constexpr unsigned short MAX_TEMPO = 400;
inline constexpr unsigned short DEFAULT_TEMPO = 138;
